# Йди на github.com → New repository → назви "elevate-app" → Public
git init
git add .
git commit -m "Initial backend implementation"
git remote add origin https://github.com/ТВІЙ_НІКНЕЙМ/elevate-app.git
git push -u origin main
